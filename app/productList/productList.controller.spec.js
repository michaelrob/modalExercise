'use strict';

describe('modalExercise.productList module', function() {

  beforeEach(module('modalExercise.productList'));

  describe('productList controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var productList.controller = $controller('productList.controller');
      expect(productList.controller).toBeDefined();
    }));

  });
});
