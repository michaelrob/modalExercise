'use strict';

describe('modalExercise.taskList module', function() {

  beforeEach(module('modalExercise.taskList'));

  describe('taskList controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var taskList.controller = $controller('taskList.controller');
      expect(taskList.controller).toBeDefined();
    }));

  });
});
